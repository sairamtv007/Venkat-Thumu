/**
 * 
 * This java code is to play a number-guessing game. The game works as follows
 * The user choose  a number  in his mind range 1-100 and types "ready". to indicate to the computer that he is ready to begin playing.
 * The Computer asks  a series of  questions to arrive at the number the user has in mind. the user can only respond with "higher","lower",or "yes"	.
 * The game ends when the user responds with "yes" or "end".
 */
package com.collgeboard;


import java.util.Random;
import java.util.Scanner;

/**
 * @author Venkat Thumu
 *
 */
public class NumberGuess {
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		String ready = null;
		NumberGuess number = new NumberGuess();
		System.out.println(" WELCOME TO NUMBER GUESSING GAME Enter Ready To start the Game:");
		Scanner scan= new Scanner(System.in);
		ready=scan.nextLine();
		if(ready.equalsIgnoreCase("ready")){
		number.guess();
		}
		
	}
/**
 * This Method will check the userGuessed Number based on system generated randomNumber.
 * if the randomNumber is equal to userGuessedNumber user will enter yes/end to stop the game.
 * if the randomNumber is less than to userGuessedNumber user will enter low to continue generate Random Numbers.
 * if the randomNumber is greaterThan to userGuessedNumber user will enter high to continure generate Random Number.
 * @return
 */
	public int  guess() {
		String userInput;
		Scanner scan = new Scanner(System.in);
		int guessedValue=guessNumber();
		userInput = scan.nextLine();
		if (userInput != null || ! ("".equals(userInput))) {
			System.out.println("User:" + userInput);
			if (userInput.equalsIgnoreCase("yes")
					|| userInput.equalsIgnoreCase("end")) {
				System.out.println("You Won, Game End!!");

			} else if (userInput.equalsIgnoreCase("low")) {
				guess();
			} else if (userInput.equalsIgnoreCase("high")) {
				guess();

			}
		}
		else{
			System.out.println("User input missing");
		}
	//	}
		return guessedValue;
		
	}
/**
 * This method will generate and return randomNumbers.
 */
	public int guessNumber() {
		Random rand = new Random();
		int randomNumber = rand.nextInt(10) + 1;
		System.out.println("Computer:Is the number:" + randomNumber);
		return randomNumber;
	}

}
			

	


